package com.volt.workout;

/** How a set of this exercise is measured; the logger renders its inputs from this. */
public enum MeasurementType {
    REPS_WEIGHT,
    DISTANCE,
    DURATION,
    REPS_ONLY
}
